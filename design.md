# ECHO 디자인 시스템 (v2 — 폰트 + 카드 캐러셀 + 음성 녹음 화면)

## 타이포그래피

- **Pretendard v1.3.9** ([orioncactus/pretendard](https://github.com/orioncactus/pretendard/releases/tag/v1.3.9))를
  앱 전체 기본 폰트로 적용. `public/fonts/pretendard/`에 dynamic-subset 빌드(`woff2-dynamic-subset` +
  `pretendardvariable-dynamic-subset.css`)를 그대로 복사해두고 `index.html`에서 `<link>`로 로드한다 —
  실제 쓰는 글자의 유니코드 서브셋만 내려받아서(체감상 몇십 KB 단위) 무거운 로케일 미포함 파일을
  통째로 받지 않는다.
- `src/index.css`의 Tailwind v4 `@theme` 블록에서 `--font-sans`를 `'Pretendard Variable', Pretendard, ...`로
  재정의하고 `body`에 명시적으로 적용 — Tailwind v4의 프리플라이트가 `--font-sans`를 기본값으로 쓰는
  동작에 기대는 동시에, 명시적으로도 한 번 더 못박아둔다.

## 카드 캐러셀 (Vertical Stacked Card Carousel)

`ref.jpg`/`cardscroll.jpg` 스타일의 "화면 중앙에 가까운 카드가 확대·선명해지고 멀어질수록 작아지며
겹쳐 보이는" cover-flow 인터랙션. 이전 버전(탭하면 그 카드만 펼쳐지는 방식)은 폐기하고, 스크롤 위치
기반의 연속적인 scale/opacity/blur 애니메이션으로 교체했다.

- **범용 컴포넌트**: `src/components/CardStackCarousel.tsx` — `items`/`renderItem`/`getKey`/`maxVisible`/
  `activeIndex`/`onActiveChange`/`cardHeight`/`overlap` props를 받는 제네릭 컴포넌트. 엔트리에 종속되지
  않아 다른 데이터로도 재사용 가능.
- **엔트리 어댑터**: `src/components/EntryCardStack.tsx` — 위 컴포넌트를 감싸서 그라디언트 카드
  렌더링만 얹는다. `EntriesPage`는 이 어댑터를 그대로 쓴다. (`TAG_GRADIENTS` 기준 색상은
  2026-09-02 "컬렉션 통일 + 카드 색상 팔레트" 절에서 `CARD_COLOR_GRADIENTS`로 대체됨.)
- **스크롤/스냅**: 커스텀 물리 엔진 대신 실제 `overflow-y: auto` + 네이티브 CSS
  `scroll-snap-type: y mandatory`를 쓴다 — 마우스 휠/터치 스와이프/모멘텀이 전부 브라우저가
  기본 제공하는 것이라 별도 구현이 필요 없고, 가볍고 안정적이다. 스크롤바는 숨김.
- **activeIndex 계산**: DOM을 매 프레임 읽는 대신, `스크롤 위치 → 카드 간격(step)으로 나눈 순수 산술`로
  각 카드의 "중앙까지의 거리"를 구한다(요청된 "IntersectionObserver 또는 거리 계산" 중 후자 방식).
  카드가 수백 개로 늘어나도 계산 비용이 늘지 않는다.
- **가상화**: 활성 카드 기준 ±5칸 밖의 카드는 아예 렌더링하지 않고, 위아래에 빈 스페이서로 스크롤
  높이만 유지한다 — 카드 개수가 아무리 많아져도 DOM 노드 수는 항상 일정하다.
- **거리별 스타일**: `scale = 1 - 0.04*|d|`, `opacity = 1 - 0.22*|d|`, `blur = max(0, (|d|-0.4)*1.4)px`,
  `z-index = 1000 - round(|d|*10)` (요청된 예시 수치와 동일한 계단: 1칸 0.96, 2칸 0.92, 3칸 0.88).
- **탭 동작**: active가 아닌 카드를 탭하면 캡처 단계에서 클릭을 가로채 "그 카드를 중앙으로 스크롤"만
  하고 내부 링크 이동은 막는다. 이미 중앙(active)인 카드를 탭하면 정상적으로 상세 화면으로 이동한다.

## 컬러 토큰

참고 이미지: `ref.jpg`(iOS 위젯형 카드 스택), `녹화탭.jpg`(따뜻한 그라디언트 음성 녹음 화면).
전체 앱 리디자인이 아니라 **아래 두 화면에만** 이 토큰을 적용한다. 기록 선택·타이핑/저장정보/
목록 필터·정렬/기록 상세 등 나머지 화면은 기존 slate 톤 미니멀 스타일을 그대로 유지한다.
(로그인 화면과 기록 선택(choice) 단계는 2026-08-30 로그인/회원가입 리디자인부터 예외 —
아래 "로그인/기록 선택 화면의 그라디언트 포인트" 절 참고.)

## 원칙

- 기존 태그 시스템(`협업/갈등/주도성/실패/성취/문제해결` 6종, `tagColors.ts`)을 색의 근거로 삼는다.
  `ref.jpg`처럼 위젯마다 자의적인 색을 쓰지 않고, **엔트리의 첫 번째 태그가 카드의 그라디언트를 결정**한다.
  태그가 없으면 중립 그레이 그라디언트. (이 항목은 2026-09-02 "컬렉션 통일 + 카드 색상 팔레트"
  절에서 대체되었다 — 태그 자동 매핑이 아니라 저장 시점에 사용자가 5색 팔레트에서 직접 고른다.)
- 나머지 앱 크롬(네비게이션, 폼, 버튼)은 다크모드로 바꾸지 않는다. `ref.jpg`는 다크 배경이지만,
  ECHO는 라이트 배경 위에 카드 자체만 화려한 그라디언트로 떠 있는 형태로 가져간다 (iOS 위젯이
  라이트 홈스크린 위에서도 화려하게 보이는 것과 동일한 논리). (이 항목은 카드 캐러셀 v2 시점의
  결정이며, 이후 "다크모드 전환" 절에서 앱 크롬 전체가 다크 테마로 전환되면서 대체되었다.)

## 컬러 토큰

### 태그별 카드 그라디언트 (`TAG_GRADIENTS`, `src/lib/tagColors.ts`)

> 이 절은 2026-09-02 "컬렉션 통일 + 카드 색상 팔레트" 절로 전면 대체되었다 — `TAG_GRADIENTS`는
> 삭제됐고, 카드 색은 태그가 아니라 저장 시점에 사용자가 고른 `CARD_COLOR_GRADIENTS`를 쓴다.
> (히스토리 보존을 위해 원문을 남겨둔다.)

| 태그 | 그라디언트 | 근거 |
|---|---|---|
| 협업 | `from-sky-500 to-blue-600` | 기존 파랑 계열 유지, 채도만 올림 |
| 갈등 | `from-rose-500 to-red-600` | 기존 빨강 계열 |
| 주도성 | `from-amber-400 to-orange-500` | 기존 앰버 계열 |
| 실패 | `from-slate-600 to-slate-800` | 유일하게 무채색 — "실패"를 축하하는 톤으로 보이지 않게 의도적으로 차분하게 유지 |
| 성취 | `from-emerald-400 to-green-600` | 기존 초록 계열 |
| 문제해결 | `from-violet-500 to-purple-600` | 기존 보라 계열 |
| (태그 없음) | `from-slate-400 to-slate-500` | 중립 폴백 |

### 음성 녹음 화면 배경 (`녹화탭.jpg` 참고)

> 이 절은 2026-09-01 녹화화면 리디자인으로 전면 대체되었다 — 아래 "녹화화면 리디자인" 절 참고.
> (히스토리 보존을 위해 원문을 남겨둔다.)

- 배경: `bg-gradient-to-b from-orange-100 via-rose-100 to-pink-200` (라이트 톤 — 원본은 더 진하지만
  ECHO 앱 전체가 라이트 배경이라 톤을 낮춤)
- 중앙 마이크 버튼: `bg-gradient-to-br from-orange-400 to-pink-500`, 원형, `shadow-lg`
- 파형 막대: 배경이 밝아졌으므로 원본(흰 막대)과 달리 대비를 위해 `bg-white/90` + `shadow-sm`
- 텍스트: 원본은 옅은 배경 위 흰 글씨지만, 우리 배경이 더 밝기 때문에 가독성을 위해
  `text-slate-700`/`text-slate-900` 유지 (원본 그대로 흰 글씨를 쓰면 밝은 배경에서 대비 부족)

## 카드 스택 스펙 (`src/components/EntryCardStack.tsx`)

> 아래 배지·날짜 색상은 이후 스크림 도입(다크모드 전환)과 2026-09-02 카드 색상 팔레트 교체를
> 거치며 바뀌었다 — 태그 배지는 `bg-white/20`이 아니라 `bg-black/40 text-white`, 날짜는
> `text-white/70`이 아니라 `text-white`(불투명)다. 실제 최신 스펙은 코드와 "컬렉션 통일 + 카드
> 색상 팔레트" 절을 따른다. (히스토리 보존을 위해 원문을 남겨둔다.)

`ref.jpg`의 "겹쳐진 카드 중 하나만 크게 펼쳐지는" 구조를 그대로 따르되, 색은 태그 그라디언트로:

- 모서리: `rounded-3xl` (기존 `rounded-lg`보다 훨씬 둥글게 — 원본의 부드러운 느낌)
- 겹침: 접힌 카드는 `-mt-11`(44px)로 이전 카드 위에 포개짐, 원본처럼 촘촘한 스택감
- 접힌 카드: 그라디언트 배경 + `text-white font-bold`, 제목 한 줄만 (padding `px-5 py-4`)
- 펼쳐진 카드: 같은 그라디언트가 이어지며 `p-5`로 확장, 흰색/반투명 흰색 텍스트 계층
  (제목 `text-white`, 본문 `text-white/90`, 날짜 `text-white/70`)
- 태그 배지(펼쳐진 카드 내부): 그라디언트 위에서 기존 `TAG_COLORS` 파스텔 배지는 대비가 안 나오므로
  `bg-white/20 text-white` 유리질(glass) 배지로 교체
- "상세 보기" 버튼: `bg-white text-slate-900` 필(pill) 버튼 — 그라디언트 위에서 가장 눈에 띄는 대비
- 그림자: `shadow-lg shadow-black/10`로 겹친 카드 사이 분리감 확보 (원본은 어두운 배경이라 그림자가
  거의 안 보이지만, 우리는 라이트 배경이라 그림자가 분리 신호로 필요) (이 라이트 배경 전제는 "다크모드
  전환" 절에서 무효화됐지만, `shadow-lg shadow-black/10` 자체는 카드 위 반투명 그라디언트 요소이므로
  다크 배경에서도 분리 신호로 유효해 그대로 유지했다.)

## 로그인/기록 선택 화면의 그라디언트 포인트 (2026-08-30 추가)

`docs/superpowers/specs/2026-08-30-login-signup-redesign-design.md` 플랜에 따라, 아래 두 화면은
"적용 범위 밖" 목록에서 빠지고 **포인트 요소에 한해서만** 기존 `orange-400 → pink-500` 그라디언트를
재사용한다:

- **로그인 화면** (`src/pages/LoginPage.tsx`): 로고(`Logo.tsx`) 아이콘, 로그인/회원가입 제출 버튼(CTA)
- **기록 선택 화면** (`RecordPage.tsx`의 choice 단계): 로고, 입력 방식 아이콘 배지

배경은 그대로 `bg-slate-50`/흰색을 유지하며, **그라디언트를 전면 배경으로 쓰지 않는다** — 카드 캐러셀/
음성 녹음 화면처럼 화면 전체를 물들이는 방식이 아니라, 로고·CTA·아이콘 배지 등 포인트 요소에만 좁게
적용해 나머지 slate 톤 미니멀 스타일과 공존시킨다. (이 항목도 "다크모드 전환" 절 시점에 대체됨 —
로그인/기록 선택 화면 배경은 `bg-slate-950`으로 전환되었다.)

## 적용 범위 밖 (이번에 안 함)

- 기록 화면(타이핑·저장정보)/목록 상단 필터·정렬 UI/기록 상세/패턴 화면의 색상 체계
  (로그인 화면과 기록 선택 단계는 위 절의 예외를 참고) (이 항목은 "다크모드 전환" 절에서 무효화됨 —
  나열된 화면 모두 다크 테마로 전환되었다.)
- 선택 모드(다중 선택) 그리드 카드 — 기존 흰 배경 + 체크 UI 그대로 유지 (그라디언트 카드에 체크박스를
  얹으면 선택 상태 대비가 애매해지므로, 선택 모드는 기존 스타일 유지) (이 결정은 "다크모드 전환" 절에서
  뒤집혔다 — 실제로는 선택된 카드에 그라디언트 + 체크마크를 적용했다. 다크 테마 전환으로 앱 전체가
  어두워지면서 시각적 일관성이 우선순위가 되었고, 체크박스 대비 우려는 그라디언트 위에 `border-white
  bg-white` 체크마크를 유지하는 것으로 해결했다.)

## 다크모드 전환 (2026-08-31 완료)

앱 전체 크롬(네비게이션, 폼, 카드, 버튼, 본문 텍스트)을 다크 테마로 전환 완료. 위 절에서 예외로 뒀던
로그인·기록 선택 화면의 그라디언트 포인트와, 음성 녹음 화면의 화려한 그라디언트 배경 위 반투명(glass)
요소들은 의도적으로 밝은 톤을 유지한다. 전체 색상 매핑 표와 화면별 결정 근거는
`docs/superpowers/specs/2026-08-30-dark-theme-conversion-design.md` 참고.

## 녹화화면 리디자인 (2026-09-01 완료)

Figma `ECHO` 파일의 `녹화화면` 프레임을 반영해 `RecordPage`의 voice 단계를 다시 만들었다.
전체 결정 근거는 `docs/superpowers/specs/2026-09-01-record-screen-redesign-design.md` 참고.

### 배경

`bg-gradient-to-b from-orange-100 via-orange-400 to-orange-700` — 위(연한 피치)에서 아래(진한
오렌지)로 흐른다.

**대비를 볼 때 반드시 세로 위치를 기준으로 계산할 것.** `via-orange-400`은 컨테이너 높이의 50%
지점이라, 화면 위쪽 절반은 밝은 색이고 흰 텍스트는 대략 92% 아래로 내려가야만 4.5:1을 넘긴다.
"하단이 orange-700이니 흰 글씨는 괜찮다"고 뭉뚱그리면 틀리며, 이 화면은 같은 실수로 이미 두 번
되돌린 이력이 있다.

따라서 **흰 텍스트는 화면 맨 아래 힌트에만** 쓰고, 그 위 본문(상단 바, 상태 라벨, 타이머, 대본,
마이크 오류)은 전부 `text-slate-800`이다 — 레퍼런스가 상단 문구를 어두운 회색으로 쓴 것과 같은
논리. 요소별 실측값은 스펙 문서의 표 참고 (타이머 ~8.1:1, 대본 ~7.1:1).

하단 컨트롤 3개는 크기만 다르고 전부 솔리드 `bg-white` + `text-orange-700`이다(글리프 5.18:1,
버튼 경계 3.90:1). 좌우를 반투명 `bg-white/25`로 두면 배경을 밝히기만 해서 흰 아이콘이 2.74:1까지
떨어진다. 상단의 반투명 유리 버튼은 `bg-white/60 border-white/80 text-slate-800`.

### 파형 (`VoiceWaveform.tsx` + `standingWave.ts` + `smoothPath.ts`)

Figma 프레임 형태를 따른다 — **얇은 가로선이 화면을 가로지르고, 그 위로 좁은 봉우리가 솟고,
선 아래로 희미하게 반사된다.** 선 아래를 통째로 채우지 않는다. 채우면 화면에 큰 네모 덩어리가
얹힌 것처럼 보인다.

**흐르지 않는다.** 이력을 오른쪽에서 왼쪽으로 미는 방식(오디오 편집기)이 아니라
**정상파(standing wave)** 로 만든다 — 각 성분이 `sin(kx)·cos(ωt)` 꼴이라 공간 모양과 시간 진동이
분리돼 있어, 패턴이 옆으로 이동하지 않고 제자리에서 진폭만 오르내린다. `sin(kx − ωt)` 꼴로 쓰면
진행파가 되어 흘러간다. 여기에 가장자리에서 0이 되는 창 함수를 곱해 양 끝을 선에 고정한다.

정상파의 **절댓값**을 쓰는 게 중요하다 — 부호를 그대로 쓰면 선 아래로 파고드는 넓은 물결이 되어
Figma 형태와 달라진다. 절댓값을 쓰면 마디가 고정되고 그 사이에 좁은 봉우리만 생긴다.

이 두 성질("양 끝이 선에 붙는다", "옆으로 밀려가지 않는다")은 `standingWave.test.ts`가 고정한다.
흐르는 구현으로 되돌아가면 상관관계 테스트가 깨진다.

`smoothPath.ts`는 값 배열을 Catmull-Rom → cubic bezier로 변환하는 순수 함수다.

ambient 시각화(아래 절)와 **함께** 얹힌다. 둘은 역할이 다르다 — ambient는 지금 이 순간의 소리를
공간으로, 파형은 소리의 세기를 선 위 봉우리로 보여준다. 채움은 반투명 흰색 3겹이며, 진폭 배율과
시간 위상만 다르다.

### 하단 컨트롤 3버튼과 상태 전이

녹화 화면은 **녹음 중 / 정지 / 완료** 3상태를 가지고, **가운데 큰 버튼이 녹음 토글을 전담**한다:

| 상태 | 왼쪽 (h-12) | 가운데 (h-28) | 오른쪽 (h-12) |
|---|---|---|---|
| 녹음 중 | `🗑` 삭제 | `■` 정지 | `✓` 완료 |
| 정지 | `🗑` 삭제 | `🎤` 시작/이어 녹음 | `✓` 완료 |
| 완료 | `🗑` 삭제 | `저장` | `✓` (비활성) |

가운데가 시작/정지를 모두 맡으므로 별도의 `⏸`/`▶` 버튼은 두지 않는다(역할이 겹친다).
정지 상태의 아이콘은 첫 화면(기록 선택)의 큰 녹음 버튼과 같은 `MicIcon`이라, "이걸 누르면
녹음이 시작된다"는 의미가 두 화면에서 동일하게 읽힌다.

`✓`는 녹음만 멈추고 **화면을 전환하지 않는다** — 사용자가 받아쓴 내용을 확인한 뒤 `저장`을
눌러야 저장 정보 입력으로 간다(2단계 확인). 대본이 비어 있으면 `✓`와 `저장`이 모두 비활성화되고
`먼저 녹음해주세요` 힌트가 뜬다.

정지와 완료를 별도 상태로 둔 이유: `✓`가 화면을 전환하지 않으므로 "녹음을 멈춘다"는 동작만으로는
정지와 완료가 구분되지 않는다. `voiceDone` 플래그로 "사용자가 끝냈다고 선언했는가"를 따로 들고
있어야 가운데 버튼이 `🎤` → `저장`으로 바뀌는 시점이 결정된다.

**알려진 제약**: 완료 상태에서는 이어 녹음할 수단이 없다(가운데가 `저장`으로 점유됨).
대본 편집은 계속 가능하고, 다시 녹음하려면 `🗑`로 버리고 시작해야 한다.

### 녹화 화면 진입 시 자동 시작 여부

| 진입 경로 | 자동 시작 |
|---|---|
| 첫 화면의 녹음 버튼 (`goToVoice`) | O — 버튼 문구가 "눌러서 바로 녹음 시작"이다 |
| 타이핑 화면의 `음성으로` (`switchTypingToVoice`) | O — 녹음하겠다는 명시적 행동 |
| 저장 정보 단계의 `← 다시 녹음` (`backToSource`) | **X — 정지 상태로 진입** |

`← 다시 녹음`만 정지로 들어간다. 되돌아온 직후 주변 소리가 그대로 받아쓰기에 섞이는 걸 막고,
사용자가 준비됐을 때 가운데 버튼으로 시작하게 하기 위함이다.

타이핑 전환 버튼은 하단에서 상단 우측으로 옮겼다(`KeyboardIcon`).

### 타이머

`speech.isRecording`이 true인 동안에만 흐른다. 틱마다 고정값을 더하지 않고 **실제 벽시계
(`Date.now`)로 계산**하며, 일시정지 시점의 경과분만 ref에 접어둔다 — 브라우저가 백그라운드 탭의
`setInterval`을 1초로 묶기 때문에 고정값 누적 방식은 화면이 꺼진 동안 시간이 크게 어긋난다.

일시정지하면 멈추고, 이어 녹음하면 리셋하지 않고 멈춘 지점부터 계속된다 — 받아쓰기 내용이
이어붙는 `useSpeechInput`의 동작과 일관되게. 같은 이유로 저장 정보 단계에서 `← 다시 녹음`으로
돌아올 때도 타이머는 리셋하지 않는다(그 기록의 누적 녹음 시간이라는 의미를 유지). 리셋은
`goToVoice`(새 녹음 시작)와 `cancelVoice`(버리기)에서만 일어난다.

표시는 `formatDuration(ms) → "M:SS"`. 녹음 상한은 두지 않는다.

### 브라우저가 임의로 끊는 녹음 (`useSpeechInput`)

Chrome은 `continuous: true`여도 침묵이 이어지면 인식 세션을 스스로 종료한다. 이걸 방치하면
사용자는 멈춘 적이 없는데 화면만 "일시정지됨"으로 바뀌고, 그 뒤에 한 말이 전부 유실된다.
`wantsRecordingRef`로 "사용자가 녹음 중이길 원하는 상태인지"를 따로 들고 있다가, 사용자가 멈춘 게
아니면 `onend`에서 곧바로 이어서 재시작한다.

또한 `stop()`은 `.stop()` 호출 전에 `onend`/`onerror` 핸들러를 떼어낸다 — 이 이벤트들은 비동기로
날아오므로, `⏸ → ▶`를 빠르게 연타하면 예전 세션의 `onend`가 새로 시작된 세션의 상태를 덮어써
"녹음 중인데 화면은 일시정지"인 어긋난 상태가 만들어진다.

마이크 훅의 `start()`도 멱등하게 만들었다 — `stop()` 없이 두 번 불리면 이전 `MediaStream`이
참조를 잃은 채 남아 마이크가 켜진 상태가 된다.

## ambient 음성 시각화 (2026-09-01)

녹화 화면의 파형을 걷어내고, 영화 Her의 인터페이스처럼 **배경에서 조용히 살아 있는** 시각화로
교체했다. 전체 결정 근거는 `docs/superpowers/specs/2026-09-01-ambient-voice-redesign-design.md` 참고.

### 왜 파형을 버렸나

막대든 곡선이든 "볼륨 이력을 가로축에 늘어놓은 그래프"는 본질적으로 오디오 편집기의 표현이다.
시간축을 버리고 **지금 이 순간의 소리를 공간으로** 표현하는 쪽으로 개념을 바꿨다.

### 구성 (`AmbientVoiceField.tsx` + `voiceEnergy.ts` + `useMicAnalyser.ts`)

- 저/중/고 주파수 대역에 각각 묶인 빛 덩어리 4개가 배경에 떠 있다. 위치·크기·색·주기·반응 강도가
  전부 달라 좌우 대칭이 생기지 않는다. `blur(44px)`로 크게 번뜨려 원반이 아니라 빛으로 읽힌다.
- **반복되지 않는 숨쉬기**: `breathe()`가 `sin(t/P)`에 `sin(t/(P·√2))`를 섞는다. √2가 무리수라 두
  성분이 정확히 다시 겹치는 시점이 없어 준주기적으로 영원히 같은 모양이 반복되지 않는다. 덩어리별
  주기도 서로 나누어떨어지지 않게 잡았다.
- **균일 확대 금지**: `scale(s)` 하나로 키우면 실루엣이 항상 완전한 원이라 기계적으로 보인다.
  `scaleX`/`scaleY`를 서로 다른 위상으로 흔들어 눌렸다 펴지게 하고, `rotate()`로 조금씩 기울여
  축이 화면과 나란한 "정직한" 도형으로 읽히지 않게 한다.
  `border-radius`를 매 프레임 바꾸는 방법도 검토했지만 **쓰지 않는다** — 그건 페인트를 무효화해
  44px 블러를 매 프레임 다시 계산하게 만든다. `transform`은 합성 단계에서만 처리되므로 공짜이고,
  블러가 이미 외곽선의 미세한 차이를 지워버리기 때문에 시각적 차이도 거의 없다.
- **이퀄라이저 방지 3중 장치**: AnalyserNode의 `smoothingTimeConstant = 0.85`, 어택은 빠르고
  릴리즈는 느린 `followEnvelope`, 그리고 `envelopeCoefficient`.
- **주사율 독립**: 엔벨로프 계수는 60fps 한 프레임 기준값이다. 그대로 매 프레임 적용하면 120Hz
  화면에서 두 배 빨라져 정확히 "이퀄라이저 느낌"이 나고 30fps에서는 뭉개진다.
  `envelopeCoefficient(c, dt)`가 실제 경과 시간으로 환산해 어느 주사율에서도 같은 시간 상수를 준다.
- **조용할 땐 정말 조용하게**: 기본 `minDecibels`(-100)는 범위가 너무 넓어 조용한 방의 암소음도
  눈금 40%까지 올라오고, 자동 게인 보정은 그걸 더 끌어올린다. `minDecibels = -75` /
  `maxDecibels = -20` / `autoGainControl: false`로 무음과 발화를 가른다.
  무음 차단의 주된 장치는 **`minDecibels`**다 — 잡음이 실제로 존재하는 dB 도메인에서 자르는 게
  원리적으로 맞는 자리다. `voiceEnergy.ts`의 `NOISE_FLOOR`는 남은 잔여분만 다듬는 보조 장치이고
  값이 작아야 한다(현재 0.06). 여기서 크게 자르면 dB 창과 이중으로 깎여, 자동 게인을 끈 상태의
  노트북 내장 마이크처럼 입력이 작은 환경에서 말해도 반응이 거의 없어진다.

### 레이아웃 — 컨트롤이 화면 밖으로 나가지 않게

높이를 `100vh`가 아니라 **`100dvh`**로 잡는다. iOS Safari의 `100vh`는 주소창을 무시한 "큰 뷰포트"
기준이라, 고정 높이로 쓰면 하단 컨트롤이 툴바 아래에 깔려 손이 닿지 않는다.

그리고 본문은 스크롤되게 두고 컨트롤 줄은 `sticky bottom-0`으로 바닥에 붙인다. 가로 모드처럼
화면이 낮은 경우에도 **녹음을 멈출 수단이 사라지지 않는 것**이 이 화면의 최우선 제약이다.
(고정 높이 + `overflow-hidden` 조합은 스크롤로 복구할 방법조차 없어서 더 나쁘다.)

### React 렌더 경로를 타지 않는다

이전 `useMicLevel`은 매 프레임 `setState`를 호출해 화면 전체를 초당 60번 리렌더했다.
`useMicAnalyser`는 상태를 들지 않고 `AnalyserNode`만 ref로 넘기며, `AmbientVoiceField`가 자기 rAF
루프에서 DOM 스타일만 직접 쓴다.

### 대비 — 이 화면 네 번째 점검

배경 `#fdf4ec` 위에 반투명 파스텔 덩어리 4개. **모든 덩어리 색이 밝은 파스텔**이라 어떤 조합으로
겹쳐도 합성 결과가 밝게 유지되고, 그 위 어두운 글씨의 대비가 덩어리 위치·크기와 무관하게
보장된다. 이게 "배경에 은은하게 깔리되 텍스트를 방해하지 않는다"를 성립시키는 핵심 장치이므로,
**덩어리 색을 어둡게 바꾸면 이 보장이 깨진다.**

네 덩어리가 최대로 겹친 최악의 경우 기준: 타이머·대본 `slate-800` 8.7:1, 상태 라벨·힌트
`slate-700` 6.2:1, placeholder `slate-600` 6.2:1, 포커스 링 3.2:1 — 전부 기준 통과.

불투명도의 숨쉬기 성분은 `baseOpacity`에 **곱해서 내리는** 형태다(`baseOpacity * (0.88 + b*0.12)`).
더하는 형태로 바꾸면 최댓값이 올라가 위 대비 계산의 최악값 전제가 깨지므로 주의.

한 가지 **의도적으로 받아들인 예외**: 흰 버튼의 `ring-1 ring-slate-900/15`는 1.36:1로 3:1 기준을
만족하지 않는다. 3:1을 넘기려면 `/50`까지 진해져야 하는데 파스텔 배경 위에 뚜렷한 검은 테두리가
생겨 이 화면의 톤과 부딪힌다. 버튼 안 아이콘이 흰 바탕 위 9.4:1이라 식별은 아이콘이 담당하며,
링은 장식이다 — "링이 경계 대비를 보장한다"고 가정하지 말 것.

## 하단 아이콘 네비게이션 (2026-09-02)

상단 텍스트 네비게이션을 없애고 화면 아래 아이콘 네비게이션으로 옮겼다. Figma 메모
"nav바 옵션들이 아래에 배치"와 카드 레퍼런스 이미지를 따른다. 전체 결정 근거는
`docs/superpowers/specs/2026-09-02-bottom-nav-design.md` 참고.

| 탭 | 경로 | 아이콘 |
|---|---|---|
| 기록 | `/` | `MicIcon` (녹화 화면 가운데 버튼과 같은 글리프) |
| 내 경험 | `/entries` (하위 경로 포함) | `CardsIcon` |
| 패턴 | `/insights` | `PulseIcon` |
| 내 정보 | `/profile` | `UserIcon` |

- `bg-slate-950` + `border-t border-slate-800`, 활성 `text-slate-50`, 비활성 `text-slate-400`
  (`slate-500`은 slate-950 위에서 4.25:1로 기준 미달이라 쓰지 않는다)
- `기록` 탭은 반드시 `<Link to="/">`로 둔다. 같은 경로로의 이동이 replace로 처리되며 새
  `location.key`를 발급하고, `RecordPage`가 그걸 "첫 화면으로 돌아가라" 신호로 쓴다.
  `<button>`+`navigate`로 바꾸면 이 동작이 깨지고 히스토리에 중복 항목이 쌓인다.

### 높이 계약 — 새 화면을 추가할 때 반드시 지킬 것

네비게이션 높이는 `index.css`의 **`--bottom-nav-total` 한 곳에서만** 정의한다
(`--bottom-nav-height` + iOS 홈 인디케이터 영역). 값을 화면마다 하드코딩하면 어느 한 곳에서
콘텐츠가 가려도 눈치채기 어렵다.

모든 화면은 둘 중 하나를 해야 한다:

- **스크롤형**: `pb-[calc(var(--bottom-nav-total)+1.5rem)]`
- **전체화면형**: `h-[calc(100dvh-var(--bottom-nav-total))]`

`100vh`가 아니라 **`100dvh`**를 쓴다 — iOS Safari의 `100vh`는 주소창을 무시한 높이라
하단이 툴바에 깔린다. `env(safe-area-inset-*)`가 실제로 값을 갖게 하려면 `index.html`의
viewport meta에 `viewport-fit=cover`가 있어야 한다(없으면 항상 0으로 계산된다).

**화면에 고정 요소가 하나 더 있으면 그것까지 비켜야 한다.** 내 경험 탭의 일괄 추가 바가
그 경우인데, 바 높이가 "새 컬렉션 만들기" 선택 여부에 따라 달라지므로 `ResizeObserver`로
실측해 목록 아래 여백에 더한다. 네비게이션 높이만 비키면 마지막 카드 줄이 바 뒤에 가린다.

## 내 정보 화면 (2026-09-02)

이메일 표시, 닉네임 편집, 기록 개수, 로그아웃(상단 nav에서 이사).

닉네임은 별도 테이블 없이 **Supabase Auth의 `user_metadata`**에 저장한다. `profiles` 테이블 +
RLS를 만드는 관례적 방식의 이점은 "다른 사용자가 내 프로필을 읽을 수 있다"인데, PRD §6이
SNS/공유/랭킹을 스코프에서 제외했으므로 그런 읽기가 생기지 않는다. 스키마 변경도, 사용자가
Supabase SQL Editor에서 마이그레이션을 돌릴 일도 없다. 프로필 항목이 여러 개로 늘어나면 옮긴다.

닉네임은 `useState`의 초기값으로만 읽고 effect로 `user`를 감시하지 않는다 — 감시하면 토큰
갱신으로 `user` 객체가 새로 오는 순간 입력 중이던 값이 덮어써진다.

## 컬렉션 통일 + 카드 색상 팔레트 + 항상-스와이프 (2026-09-02)

사용자 질문("프로젝트별과 컬렉션별이 곧 같은 거 아니냐")을 계기로 그룹 기준과 카드 색상 체계를
다시 정리했다.

### 그룹 기준을 컬렉션 하나로 통일

`project_title`(자유 텍스트, 저장 시 입력)과 `collection_id`(컬렉션 선택/생성)는 사실 같은
"묶음" 개념으로 쓰이고 있었다 — 사용자가 저장할 때 굳이 두 필드를 구분해서 채우지 않았다.
그래서:

- `RecordPage`의 "프로젝트 제목" 자유 입력칸(+ 기존 제목 자동완성)을 없앴다. 이제 저장 시점에
  **컬렉션만** 고르거나 새로 만든다.
- `project_title` 컬럼 자체는 DB에 남겨뒀다 — 옛 기록의 값을 지우지 않기 위해서다. 신규
  기록에는 더 이상 쓰지 않고, 값이 없으면 `EntryCardStack`이 '제목 없음'을 그대로 보여준다.
  처음엔 빈 자리를 컬렉션 이름으로 채우는 것도 시도했지만, 스와이프 뷰 헤더와 선택 모드
  그리드 둘 다 이미 그 컬렉션 이름을 카드 바로 옆(위)에 보여주고 있어서 카드 제목이 헤더를
  그대로 반복할 뿐 정보가 하나도 늘지 않았다(리뷰에서 지적, "미분류 표시가 헤더·카드에
  두 번 겹쳐 보인다"던 첫 문제가 이름 있는 컬렉션에서도 똑같이 재현) — 그래서 폴백 자체를
  없앴다.
- 정렬 드롭다운에서 "프로젝트별"/"월별"을 없앴다. "월별"은 애초에 최신순과 정렬 순서가
  완전히 같고 그룹 헤더만 달랐던 채였다(재정렬을 안 함) — 별도로 유지할 실익이 없었다.
- 결과적으로 `src/lib/entryGrouping.ts`의 `groupEntries()`는 이제 컬렉션 기준 하나만 받는다
  (`GroupBy` 유니언 타입 자체를 없앰). 선택 모드(다중 선택) 그리드도, 컬렉션 스와이프 뷰도
  같은 `groupEntries()` 결과를 공유한다.

### 카드 색상 — 태그 자동 매핑 → 저장 시 직접 선택

이전엔 엔트리의 첫 번째 태그로 카드 그라디언트를 자동 결정했다(`TAG_GRADIENTS`, 태그별
짙은 남색→파스텔 대각선). 이번엔 사용자가 5색 팔레트를 직접 지정했고("393960, EAAEA5,
96709A, E58F91, 5D628C"), **저장 시점에 사용자가 직접 고르는** 방식으로 바꿨다 — 태그와
카드 색이 더 이상 연동되지 않는다.

- `src/lib/tagColors.ts`: `CARD_COLOR_KEYS`(`navy`/`rose`/`plum`/`coral`/`slate`),
  `CARD_COLOR_HEX`(스와치용 원색), `CARD_COLOR_GRADIENTS`(카드용 그라디언트 클래스),
  `CARD_COLOR_LABELS`(a11y 라벨)를 새로 뒀다. `TAG_GRADIENTS`/`NEUTRAL_CARD_GRADIENT`는
  삭제했다 — `TAG_COLORS`/`TAG_COLORS_ACTIVE`(필터 칩 색)는 카드 색과 별개라 그대로 둠.
- 그라디언트는 "은은하게"라는 요청대로, 이전처럼 색상 자체가 확 바뀌는 대비가 아니라
  **같은 색을 흰색 쪽으로 16% 섞은 밝은 변형**으로만 이어지는 좁은 범위다(예:
  `#393960 → #595979`). 방향은 `to-br`로 기존 카드 스크림 축과 그대로 맞춘다.
  **주의**: `to-br` 스크림은 좌상단(제목이 앉는 자리)에서 알파가 0에 가까워, 밝은 베이스색
  (rose `#EAAEA5`, coral `#E58F91`)에서는 그것만으로 흰 텍스트 대비가 부족했다(코드 리뷰에서
  발견, 2:1대까지 떨어짐). 스크림 시작 스톱을 `from-black/0`에서 `from-black/30`으로 올려
  카드 전체에 최소 바닥을 깔아 보정했다 — 카드 색을 새로 추가/조정하는 다음 사람은 "스크림이
  있으니 안전하다"고 가정하지 말고 실제 좌상단 좌표에서 대비를 다시 계산할 것.
- DB: `entries.card_color text check (... in ('navy','rose','plum','coral','slate'))` 추가
  (`supabase/schema.sql`, 마이그레이션은 SQL Editor에서 직접 실행). 값이 없는 옛 기록은
  `DEFAULT_CARD_COLOR`('navy')로 폴백.
- UI: `RecordPage` 저장 정보 단계에 5개 정사각형 스와치를 나열 — hex는 Tailwind 임의값
  클래스가 아니라 인라인 `style`로 준다(배열을 돌며 변수로 색을 넣는 자리라 애초에 정적
  스캔 대상이 될 수 없다 — 카드 그라디언트 클래스는 리터럴 문자열이라 이 문제와 무관함).

### 컬렉션 스와이프 뷰 — 토글 없이 항상 켜짐 + 점 인디케이터

Figma "스와이프" 참고 프레임(점 3개로 몇 번째/총 몇 장인지 보여주는 사진 앱 UI)을 따라
`CollectionSwipeView`를 다시 그렸다:

- 상단 아이콘 토글을 없앴다 — Figma 메모 원문("내 경험탭에서 좌우로 스와이프하면 컬렉션별로
  넘어가게 함")대로 스와이프가 **내 경험 탭의 기본 동작**이 됐다. `EntriesPage`는 이제
  `selectMode`가 아니면 항상 `CollectionSwipeView`를 보여준다.
- 가로 스크롤 pill 바 대신, 중앙 정렬된 **컬렉션 이름 + 개수** 텍스트를 상단에 둔다. 페이지가
  바뀔 때마다 `key`를 바꿔 다시 마운트시켜 `fade-in-up-enter`(`index.css`, 아래에서
  살짝 올라오며 페이드인, 180ms — 원래 이름 `collection-label-enter`였다가 2026-09-05에
  녹음 화면 커서-이어녹음 안내에도 재사용하며 범용 이름으로 바꿨다) 애니메이션을 재생한다 —
  텍스트가 뚝 바뀌는 대신 "지금 페이지가 넘어갔다"는 걸 분명히 알 수 있다.
- 카드 아래에는 점 인디케이터를 둔다 — 활성 컬렉션만 알약 모양으로 늘어나고(`w-5`), 나머지는
  작은 원(`w-2`)이라 몇 개 컬렉션이 있는지 한눈에 예측 가능하다. 탭하면 그 컬렉션으로 바로
  이동(예전 pill 버튼의 이동 기능 유지).

## 녹음 화면 개선: 커서 이어녹음 · 컨트롤 크기 · 대역별 파형 (2026-09-05)

### 커서 위치부터 이어 녹음

일시정지 중에는 대본이 수정 가능한 `<textarea>`로 바뀐다(기존 동작). 여기에 커서를 두고
▶(이어 녹음)를 누르면, 예전엔 무조건 전체 텍스트 맨 끝에 새로 인식된 말을 붙였다 — 커서를
중간에 두고 이어 말해도 그 자리에 끼워지지 않고 끝으로 가버렸다.

- `useSpeechInput`의 `start()`가 이제 선택적으로 문자 인덱스(`insertAt`)를 받는다. 그 위치
  뒤의 텍스트를 `suffixRef`에 떼어두고, 새로 인식되는 말은 "커서 앞부분 + 새 말 + suffix"
  순서로 합친다(`src/lib/transcriptMerge.ts`의 `mergeAtCursor`, 순수 함수 + 단위 테스트).
  침묵으로 인한 자동 재시작(onend → restart) 동안에도 `suffixRef`는 그대로 유지되어, 한
  세션 안에서 재시작이 여러 번 일어나도 계속 같은 자리 앞에 삽입된다.
- `RecordPage`는 이어 녹음 버튼을 누르는 순간 `textarea.selectionStart`를 읽어 `insertAt`으로
  넘긴다. 버튼 클릭이 포커스를 텍스트영역 밖으로 옮겨도 `selectionStart` 값 자체는
  DOM에 남아있어 문제없이 읽힌다.
- 커서가 텍스트 중간에 있을 때만(끝이면 예전과 동일한 "그냥 이어붙이기"라 안내가 필요 없다)
  "커서 위치부터 녹음을 이어갑니다" 안내를 2초간 띄운다.
- 커서 자체도 파스텔 배경 위에서 잘 안 보인다는 피드백이 있어 `caret-orange-600`으로
  브랜드 강조색을 줘 진하게 보이게 했다.

### 컨트롤 버튼 크기

삭제/완료 버튼(48px)이 가운데 녹음 버튼(112px)과 비교해 지나치게 작다는 피드백으로 전부
한 단계씩 키웠다: 삭제·완료 48px→56px(아이콘 20px→24px), 가운데 마이크/정지/저장 버튼
112px→128px(아이콘도 비례해 확대).

### 파형이 실제로 음역대별로 움직이게

`VoiceWaveform`은 원래 세 겹(LAYERS) 전부가 시간 도메인 RMS(전체 음량) 하나로만 움직였다 —
위상만 다를 뿐 사실상 같은 신호였다. "음역대로 나눠서 움직이는 거냐"는 질문에 답하며,
`AmbientVoiceField`(배경 시각화)가 이미 쓰던 `voiceEnergy.ts`의 저/중/고 대역 분석
(`bandEnergies`, `getByteFrequencyData` 기반)을 그대로 가져와 세 겹을 각자 다른 대역에
물렸다. 추가로 다음 값을 올려 전체적으로 더 크게 요동치게 했다: `RecordPage`에서 파형
컨테이너 높이 `h-20`→`h-28`, 어택/릴리즈(`ATTACK`/`RELEASE`) 상향, 대역 게인(`BAND_GAIN`)
도입.

## 내 경험 탭 3종 개선: 태그 한 줄 · 컬렉션 모음 시트 · 카드 페이지 점 (2026-09-05)

### 태그 필터 한 줄 고정

`EntriesPage`의 6개 태그 칩이 `flex-wrap`이라 화면 폭에 따라 두세 줄로 들쭉날쭉했다.
`overflow-x-auto` + `shrink-0`로 한 줄 고정, 넘치면 가로 스크롤(스크롤바는 다른 가로
스크롤 영역과 동일하게 숨김)로 바꿨다.

### 컬렉션 모음 시트

컬렉션 스와이프 뷰는 한 번에 하나씩만 보여줘서, "지금 컬렉션이 뭐가 있는지" 전체를 한눈에
보려면 계속 넘겨야 했다. `EntriesPage`에 "컬렉션 모음 (N)" 버튼을 추가해(컬렉션이 2개
이상일 때만 노출), 누르면 이름+개수를 나열한 시트가 뜨고 탭하면 그 컬렉션으로 바로
넘어간다.

- `CollectionSwipeView`를 `forwardRef`로 바꿔 `scrollToKey(key)` 하나만 노출한다.
  스크롤 위치 자체를 부모로 끌어올리지 않은 이유는, 그러면 스크롤 프레임마다
  `EntriesPage`까지 리렌더되기 때문 — ref로 "이동해라" 명령만 보내는 쪽이 싸다.
- 시트는 새 컴포넌트를 만들지 않고 `RecordPage`의 `LeaveConfirmDialog`와 같은 관례(고정
  오버레이 + 카드)를 따르되, 목록이라 바텀시트 형태(`items-end`, 모바일 폭에서
  `rounded-t-2xl`)로 뒀다.

### 카드 스택 페이지 점 인디케이터

컬렉션 스와이프의 점 인디케이터와 별개로, 한 컬렉션 안에서 세로로 카드를 넘길 때도
"몇 번째 카드인지" 보여달라는 요청. 컬렉션 개수(보통 한 자리 수)와 달리 카드는 수십~수백
개로 늘어날 수 있어 전부 점으로 그릴 수 없다 — iOS `UIPageControl`의 automatic(축소) 표시를
참고해 `src/lib/pagerDots.ts`(순수 함수 + 단위 테스트)를 만들었다: 활성 카드를 중심으로 한
창(기본 7개)만 보여주고, 창의 양 끝이 실제 처음/끝이 아니면 더 작은 점으로 그려 "이 너머에
더 있다"를 암시한다.

- `EntryCardStack`이 `CardStackCarousel`의 `activeIndex`/`onActiveChange` 계약을 처음으로
  실제 사용한다 — 로컬 `activeIndex` state를 컨트롤드로 넘겨, 점을 누르면 그 카드로 바로
  스크롤도 된다.
- 카드가 1장뿐인 컬렉션은 점을 아예 숨긴다(의미가 없다).

**2026-09-05 추가 수정**: 헤더의 "선택" 텍스트 버튼을 연필/체크 아이콘 토글로,
"컬렉션 모음 (N)" 버튼을 아이콘만 남긴 원형 버튼으로 줄였다(둘 다 요청). 컬렉션 모음
팝업은 모바일 폭에서 바텀시트로 뜨던 것을 화면 크기와 무관하게 항상 가운데 팝업으로
통일했다.

목록 항목 호버 효과는 두 번 갈아엎었다: 처음엔 화살표를 opacity로 페이드인시켰는데
잘 안 보인다는 피드백이 왔다 — 원인은 팝업 배경(`slate-900`)과 호버 배경(`slate-800`)의
톤 차이가 너무 작아서였다. 배경 자체를 한 단 더 밝은 `slate-700`으로 바꾸고, 왼쪽에서
`scale-x`로 슬라이드-인하는 하늘색 강조 바를 더해 "이 항목이 활성화됐다"가 배경과
왼쪽 바 둘 다에서 분명히 드러나게 했다(화살표는 제거). 편집(선택) 모드의 컬렉션별
섹션은 회색 제목 한 줄뿐이라 경계가 흐렸던 것을, 테두리 있는 카드 블록(`rounded-xl
border`)으로 감싸고 이름 옆에 개수 배지를 붙여 어디부터 어디까지가 한 컬렉션인지
스크롤 중에도 분명하게 했다.

**2026-09-05 수정**: 처음엔 점을 스택 *아래*에 가로로 깔았는데, 기기별로 화면 높이가 달라
스택의 고정 높이(`containerHeight`, `cardHeight`·`overlap`·`maxVisible`로 계산되는 px 값)
뒤에 얹힌 그 한 줄이 화면 밖으로 밀려 "어떤 폰에서는 보이고 어떤 폰에서는 안 보이는"
문제가 있었다. 세로로 넘기는 스택인데 인디케이터가 가로로 따로 떨어져 있는 것도 어색했다.
`CardStackCarousel`과 점 목록을 `flex items-stretch` 한 줄로 묶고 점 목록을 세로
(`flex-col`)로 바꿔 해결했다 — `items-stretch`가 점 목록 열의 높이를 스택과 똑같이
맞춰주므로, 스택이 화면에 보이는 한(즉 스크롤해서 카드가 보이는 어떤 상황에서도) 점도
같은 자리에서 항상 같이 보인다. 별도의 높이 계산이나 미디어 쿼리 없이 flexbox만으로
반응형이 되는 셈.

## 패턴 탭 — 경험 별자리

경험 하나가 별 하나인 3D 우주다 (three.js, plain·R3F 없이 `ConstellationCanvas`가 명령형으로 소유).

- **군집** — `insights.evidence_entry_ids`를 뒤집어 `entry_id → cluster`를 만든다.
  energizer 근거 → 앰버, drainer 근거 → 블루그레이, 나머지 → 중립 화이트.
  양쪽 근거인 기록은 **energizer 우선**으로 한 군집에만 둔다(별 복제 금지).
- **좌표** — `layout.ts`의 `starPosition()`이 entry id 해시(FNV-1a → mulberry32)로 결정적으로
  계산한다. `Math.random()` 금지 — 새로고침해도 같은 별이 같은 자리에 있어야 회상이 된다(PRD §8).
- **연결선** — 같은 군집 안에서만. 공통 태그 1개 이상이거나 같은 컬렉션이면 후보가 되고,
  같은 컬렉션에 가중치 10을 얹어 점수순으로 채우되 **노드당 최대 3개**. 제한이 없으면 태그가
  6종뿐이라 완전그래프가 되어 화면이 선으로 덮인다.
- **군집 라벨** — three.js 스프라이트가 아니라 캔버스 위 HTML 버튼이다. 매 프레임 월드 좌표를
  화면 좌표로 투영해 `style.transform`만 갱신한다(state를 쓰면 프레임마다 리렌더된다).
  한글 폰트가 기존 UI와 같게 렌더되고, 손가락 히트박스가 확보되며, 스크린리더가 읽는다.
- **재생성 실패 시 기존 별자리를 지우지 않는다.** 우주가 통째로 사라지면 손실감이 크다.
- WebGL을 못 쓰는 기기는 `ClusterSummaryCard` 두 장을 세로로 펼친 정적 화면으로 폴백한다.

### 배경 레이어 (`ExperienceGalaxyBackground`)

깊이감은 **DOM/CSS와 WebGL로 나눠서** 만든다.

| 레이어 | 구현 | 이유 |
|---|---|---|
| 심우주 그라데이션 → 은하수 띠 → 성운/먼지 → 천체 → 비네트 | CSS `radial/linear-gradient` + `blur` | 합성 단계에서만 처리돼 GPU 비용이 사실상 0이고, WebGL 폴백 화면에서도 같은 분위기를 쓴다 |
| 먼 배경 별 2겹 (반지름 78~140) | three.js `Points` 2개 | 카메라 궤도 최대 거리가 70이라 항상 이 구각 **안쪽**에 머문다 → 시차가 공짜로 생긴다 |
| 군집 성운 | `Sprite` 3장 | 군집 위치를 색으로 암시. 3장뿐이라 비용 무시 가능 |
| 경험 별 / 연결선 / 선택 고리 | 기존 그대로 | 데이터 레이어 |

- 순수 검정이 아니라 **짙은 남색 → 검정**이다. 순수 검정은 별이 오려붙인 것처럼 납작해 보인다.
- 별 텍스처는 중심이 작고 아주 밝으며 주변은 넓고 옅다. 균일한 원(disc)이면 "점 찍은 배경"이 된다.
- 선택 고리는 별 텍스처를 크게 키운 게 아니라 **가운데가 빈 링**이다. 예전 방식은 큰 동심원
  얼룩처럼 보여 별 자체를 가렸다.
- 반짝임은 별 단위가 아니라 **레이어 단위 호흡**이다. 별마다 따로 반짝이려면 커스텀 셰이더가
  필요한데 효과 대비 비용·위험이 크다.
- 저사양 대응은 `backdrop.ts`의 두 함수가 나눠 맡는다. `backdropScale()`은 **배경 별 개수**를,
  `backgroundDetail()`은 **배경 장식의 겹 수**를 줄인다. 둘을 나눈 이유는 비용의 원천이 다르기
  때문이다 — 별은 개수, 장식은 큰 면적에 걸린 blur 레이어 수. 경험 별은 데이터라 줄이지 않는다.
- `prefers-reduced-motion`이면 자동 회전·성운 흐름·호흡·카메라 트윈·카드 슬라이드가 모두 멈춘다.
- **은하수**는 화면을 -26°로 가로지르는 정지 레이어 3겹(넓고 흐린 겹 → 좁고 밝은 겹 →
  어두운 암흑성운 겹)이다. 정지 상태라 합성 결과가 캐시돼 첫 프레임 이후 비용이 없다.
  마지막 어두운 겹은 lite에서 생략한다.
- **천체는 최대 2개** — 오른쪽 위의 작은 달, 왼쪽 가장자리의 먼 은하. lite에서는 달만 남는다.
  둘 다 비네트가 어두워지는 가장자리에 두어 가운데 분석 영역의 여백을 지킨다.
- 모든 배경 레이어의 불투명도가 0.2를 넘지 않는다. 배경은 조연이고, 경험 별과 카드가 먼저 읽혀야 한다.

### 카드 (`BottomSheet`)

별 상세와 군집 요약은 아래에서 올라오는 **높이 고정(40dvh) 시트**다.

- 높이를 고정하는 이유는 그 위에 떠야 하는 것들(전체 보기 버튼, 카메라가 비워 둘 영역)이
  카드 내용 길이에 따라 흔들리면 안 되기 때문이다. `SHEET_HEIGHT` / `CARD_RAISE` /
  `bottom-[calc(40dvh+…)]` 세 곳이 이 값의 짝이다 — 하나를 바꾸면 셋 다 바꿔야 한다.
- 스크롤은 브라우저 기본 스크롤(`overflow-y-auto overscroll-y-contain`)에 맡긴다.
  내 경험 탭 카드 스택과 같은 방식이라 모바일 관성이 그대로 살아 있다.
- 군집 카드는 시트가 아니라 **자기가 스크롤 영역을 나눈다**(`BottomSheet scroll={false}`).
  머리말(군집 이름·닫기)은 고정이고 인사이트 목록만 가운데에서 스크롤된다 — 머리말이 같이
  밀려 올라가면 지금 어느 별무리를 보고 있는지 알 수 없게 된다.
- 인사이트 한 줄은 **버튼 두 개**다. 요약을 누르면 근거 별만 밝아지고, 그 아래
  `근거 기록 N건 펼치기`를 누르면 근거 기록이 카드 안에서 펼쳐진다(구조화 결과가 있으면
  상황/행동/결과/감정, 없으면 원문 + "정리 중", 각각 `/entries/:id` 링크). 버튼을 중첩하면
  HTML이 깨지므로 형제로 둔다. 한 번 불러온 근거는 캐시하고, 재분석 시 캐시를 비운다.
- `다시 분석하기`는 카드 한 줄을 차지하던 큰 버튼에서 머리말의 작은 `↻`로 옮겼다. 자주 쓰는
  기능은 아니지만, 기록이 쌓인 뒤 다시 분석할 길이 아예 없으면 인사이트가 첫 분석 시점에
  얼어붙는다.
- 슬라이드는 `transform`만 바꿔 레이아웃을 재계산시키지 않는다.
- 카드가 열리면 카메라가 대상을 화면 위쪽으로 올린다(`CARD_RAISE = 0.4`, 반높이의 40% =
  화면 높이의 20% → 남은 60%의 한가운데). 무엇을 눌렀는지 보이지 않으면 카드 내용의 출처를
  알 수 없다. 올리는 방향은 월드 y축이 아니라 **현재 카메라 기준 화면 위 방향**이다.
- 화면 하단 가운데의 `전체 별자리 보기` 버튼이 첫 시점으로 되돌린다(카드도 함께 닫는다).
  카드가 열려 있으면 카드 바로 위로 올라간다 — 카드 뒤에 깔리면 다음 라벨을 고르러 나갈
  유일한 통로가 사라진다.

### 카메라 이동

`beginFocus(target, distance)` 하나로 별 포커스와 군집 포커스를 모두 처리한다. 보고 있던
방향은 유지하고 거리만 바꾼다 — 시점이 갑자기 뒤집히면 방향 감각을 잃는다.

- **군집 라벨을 탭하면** 그 별무리로 줌인한다(`clusterViewDistance()` = 군집 반경 × 2.4 + 3.5,
  fov 55°에서 반경을 담는 최소 거리 ≈ 1.9r에 여백을 더한 값). 같은 라벨을 다시 눌러도 다시
  이동해야 하므로 값이 아니라 **token**으로 새 요청을 구분한다.
- **트윈은 반드시 끝난다.** 수렴 조건(거리 < 0.05)에 더해 프레임 상한(120)을 두고, 사용자가
  드래그·휠을 시작하면(`controls` `start` 이벤트) 즉시 포기한다. 트윈 중에는 자동 회전을 끈다.
  이 세 가지가 없으면 트윈이 매 프레임 카메라를 덮어써서 **줌·회전이 영영 먹지 않는다** —
  실제로 자동 회전과 트윈이 서로를 밀어 수렴하지 못하는 버그가 있었다.

계산(`src/lib/constellation/`)과 렌더(`src/components/constellation/`)를 갈라 두었다.
군집 배정·연결선 제한·좌표 결정성이 가장 틀리기 쉬운 부분인데, 순수 함수라 WebGL 없이
vitest로 검증된다. 이 프로젝트에는 jsdom이 없으므로 **캔버스 컴포넌트는 단위 테스트하지 않는다.**

---

## 우주 리디자인 (2026-09-07)

`src/public/design-references/01~08`을 화면별 디자인 스펙으로 삼아 전체 UI를 다시 그렸다.
기능·라우팅·API·인증·녹음 로직은 건드리지 않았다 — 바꾼 것은 JSX와 스타일뿐이다.

### 컨셉

경험 = 별, 비슷한 경험 = 별자리, 군집 = 성운, 쌓인 경험 = 나의 우주.
우주는 장식이 아니라 정보 구조의 은유다. 다만 **배경보다 위젯이 우선**이다 —
사용자 행동 > 사용자 데이터 > 위젯 > 우주 장식 순으로 위계를 지킨다.

### 배경 — 화면별 이미지 (2026-09-07 3차, 최종)

절차적 생성(CSS 그라데이션 → 캔버스 회화)을 두 번 시도했지만, 사용자가 원하는 장면을
그림으로 확정해 주면서 **그 이미지를 그대로 까는 방식**으로 바꿨다. 결과도 낫고 코드도
단순하다. 절차적 모듈(`clouds` / `Starfield` / `Planet` / `ShootingStars`)은 제거했다 —
필요하면 git 히스토리에서 되살릴 수 있다.

원본은 장당 941×1672 PNG, 약 2.3MB다. 그대로 쓰면 8장에 17MB라 모바일에서 못 쓴다.
WebP(quality 72)로 변환해 `public/bg/*.webp`에 둔다 — **장당 50~130KB, 합계 786KB**이고
화면 하나가 한 장만 받는다. 변환 스크립트는 일회성이라 저장소에 두지 않았다
(PIL로 resize + WEBP quality=72, method=6).

| variant | 파일 | object-position | 이유 |
|---|---|---|---|
| login | `login.webp` | center | 우상단 행성과 아래 지평선을 둘 다 살린다 |
| record-home | `record-home.webp` | center **bottom** | 지구 지평선 위에 마이크 오브가 떠야 한다 |
| recording | `recording.webp` | center | |
| archive | `archive.webp` | center | |
| detail | `detail.webp` | center **top** | 은하수 띠가 위쪽에 있다 |
| detail-starwl | `detail-starwl.webp` | center top | 탭에 따라 하늘이 바뀐다 |
| pattern | `pattern.webp` | center | |
| profile | `profile.webp` | center | 우상단 행성 가장자리가 주인공 |

이미지는 9:16인데 요즘 폰은 더 길고 데스크톱은 가로로 넓다 — `cover`로 잘릴 때
무엇을 남길지가 화면마다 다르므로 `object-position`을 variant마다 지정한다.

가독성은 **상단 스크림**으로만 지킨다(0.30~0.42). 로고·제목이 배경의 밝은 부분 위에 올 때를
대비하는 것이고, 화면 전체를 어둡게 하지는 않는다. 카드는 이미 어두운 유리라 그 위 글자는
배경과 무관하다.

배경은 `pointer-events-none`이라 위젯의 클릭을 가로채지 않는다.

패턴 탭에서는 3D 별자리의 배경 별을 `density={0.35}`로 줄인다 — 배경 사진에 이미 별이
가득해서 두 겹이 겹치면 지저분해진다.

### 공통 컴포넌트

`components/ui/` — `GlassCard`(기본은 glow 없음, `active`일 때만 약한 glow) ·
`GlassPanel` · `GradientButton`(화면당 하나) · `OutlineButton` · `CosmicIconButton`(44px) ·
`CosmicInput` · `CosmicTextarea` · `TagChip`(채운 배지가 아니라 테두리·글자에만 색).

`BottomNav`는 화면 가장자리 막대가 아니라 **떠 있는 유리 dock**이다. 활성 항목만 독립
캡슐이고 탭마다 glow 색조가 다르다(기록=코럴, 내 경험=오렌지, 패턴=바이올렛, 내 정보=핑크).
아이콘이 `currentColor`를 쓰므로 링크에 색을 주면 아이콘과 라벨이 함께 물든다.

### 유지한 것

카드 색상 5종은 그대로다 — 다만 카드를 통째로 칠하지 않고 **어두운 유리판 위 좌상단
tint**로만 남긴다. 바탕이 항상 어두우므로 흰 텍스트 대비가 카드 색과 무관하게 일정해졌고,
예전에 대비를 맞추려 넣었던 to-br 스크림(시작 스톱 35%)이 필요 없어졌다.

### 제거한 것

`AmbientVoiceField`(옛 주황 그라데이션 녹음 화면의 배경) · `ExperienceGalaxyBackground`
(SpaceScene이 대체) · `PageHeader`(화면마다 머리말 구성이 달라 각 페이지에서 직접 조립).
